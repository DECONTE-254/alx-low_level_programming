#include "main.h"

/**
 * _issupper - checks if a character is uppercase
 * @c: variable text
 * Return: Always 0
 */
int _issupper(int c)
{
	if (c >= 'A' && C <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
